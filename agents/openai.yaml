interface:
  display_name: "RightQuestion"
  short_description: "Find the question that unlocks the real goal"
  default_prompt: "Use $right-question to find the one question I should answer before deciding what to do next."
policy:
  allow_implicit_invocation: true
