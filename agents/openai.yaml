interface:
  display_name: "RightQuestion"
  short_description: "Reframe the request, then solve what really matters"
  default_prompt: "Use $right-question to identify the problem I actually need handled, then solve it or complete the work."
policy:
  allow_implicit_invocation: true
